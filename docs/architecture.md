# Architecture

> Maintained by Nirvana. Updated automatically every N sessions via /reflect.
> Last updated: never — run /reflect to generate first synthesis.

## Stack

Markdown-based Claude Code skill framework. PowerShell (`install.ps1`) and Bash (`install.sh`)
installers ship 7 global skills + a global templates cache (`~/.claude/nirvana-templates/`)
that `/light` copies into any target project.

## Project Structure

<!-- Auto-fill: run /reflect after first few sessions -->

## Key Layers

<!-- Auto-fill: run /reflect after first few sessions -->

## External Dependencies

<!-- Auto-fill: run /reflect after first few sessions -->

## Request Flow

<!-- Auto-fill: describe how a request flows through the system -->
